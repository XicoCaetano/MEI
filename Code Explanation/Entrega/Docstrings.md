## Python/23 - Return length of given string.

## Python/54 - Check if two words have the same characters.

## Python/35 - Return maximum element in the list.

## Python/27 - For a given string, flip lowercase characters to uppercase and uppercase to lowercase.

## Python/52 - Return True if all numbers in the list l are below threshold t.

## Python/3 - You're given a list of deposit and withdrawal operations on a bank account that starts with zero balance. Your task is to detect if at any point the balance of account falls below zero, and at that point function should return True. Otherwise it should return False.

## Python/6 - Input to this function is a string represented multiple groups for nested parentheses separated by spaces. For each of the group, output the deepest level of nesting of parentheses. E.g. (()()) has maximum two levels of nesting while ((())) has three.

## Python/14 - Return list of all prefixes from shortest to longest of the input string.

## Python/30 - Return only positive numbers in the list.

## Python/26 - From a list of integers, remove all elements that occur more than once. Keep order of elements left the same as in the input.

## Python/13 - Return a greatest common divisor of two integers a and b.

## Python/25 - Return list of prime factors of given integer in the order from smallest to largest. Each of the factors should be listed number of times corresponding to how many times it appears in factorization. Input number should be equal to the product of all factors.

## Python/36 - Return the number of times the digit 7 appears in integers less than n which are divisible by 11 or 13.

## Python/72 - Write a function that returns True if the object q will fly, and False otherwise. The object q will fly if it's balanced (it is a palindromic list) and the sum of its elements is less than or equal the maximum possible weight w.

## Python/38 - Takes as input string encoded with encode_cyclic function. Returns decoded string.

## Python/10 - Find the shortest palindrome that begins with a supplied string.

## Python/40 - triples_sum_to_zero takes a list of integers as an input. It returns True if there are three distinct elements in the list that sum to zero, and False otherwise.

## Python/60 - sum_to_n is a function that sums numbers from 1 to n.

## Python/62 - xs represent coefficients of a polynomial: xs[0] + xs[1] * x + xs[2] * x^2 + .... Return derivative of this polynomial in the same form.

## Python/74 - Write a function that accepts two lists of strings and returns the list that has total number of chars in the all strings of the list less than the other list. If the two lists have the same number of chars, return the first list.

## Python/119 - You are given a list of two strings, both strings consist of open parentheses '(' or close parentheses ')' only. Your job is to check if it is possible to concatenate the two strings in some order, that the resulting string will be good. A string S is considered to be good if and only if all parentheses in S are balanced. Return 'Yes' if there's a way to make a good string, and return 'No' otherwise.

## Python/120 - Given an array arr of integers and a positive integer k, return a sorted list of length k with the maximum k numbers in arr.

## Python/140 - Given a string text, replace all spaces in it with underscores, and if a string has more than 2 consecutive spaces, then replace all consecutive spaces with -.

## Python/145 - Write a function which sorts the given list of integers in ascending order according to the sum of their digits. If there are several items with similar sum of their digits, order them based on their index in original list.

## Python/159 - You're a hungry rabbit, and you already have eaten a certain number of carrots, but now you need to eat more carrots to complete the day's meals. Return an array of [total eaten carrots after your meals, carrots left after your meals]. If there are not enough remaining carrots, you will eat all remaining carrots, but will still be hungry.

## Python/84 - Given a positive integer N, return the total sum of its digits in binary.

## Python/105 - Given an array of integers, sort the integers that are between 1 and 9 inclusive, reverse the resulting array, and then replace each digit by its corresponding name from "One", "Two", "Three", "Four", "Five", "Six", "Seven", "Eight", "Nine".

## Python/126 - Given a list of numbers, return whether or not they are sorted in ascending order. If list has more than 1 duplicate of the same number, return False. Assume no negative numbers and only integers.

## Python/130 - Tribonacci sequence: tri(1) = 3, tri(n) = 1 + n/2 if n is even, tri(n) = tri(n-1) + tri(n-2) + tri(n+1) if n is odd. Return a list of the first n+1 numbers of the sequence.

## Python/153 - Given a class name and a list of extensions, find the strongest extension. The strength is: CAP (uppercase letters) minus SM (lowercase letters). Return "ClassName.StrongestExtensionName". If two extensions tie, choose the first one in the list.
